RSpec.describe Knights::Of::The::Zodiac do
  it "has a version number" do
    expect(Knights::Of::The::Zodiac::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
